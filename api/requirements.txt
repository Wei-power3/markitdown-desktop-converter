# Flask API Requirements for MarkItDown v2.4.2
Flask==3.0.0
flask-cors==4.0.0
Werkzeug==3.0.1

# Core dependencies (already in main requirements)
# These should be installed from main requirements.txt
